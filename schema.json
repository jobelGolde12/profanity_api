{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/schemas/pure_filipino.json",
  "title": "Pure Filipino Profanity",
  "description": "Schema representing a Filipino profanity word entry stored in the database.",
  "type": "array",
  "properties": {
    "id": {
      "type": "integer",
      "minimum": 1,
      "description": "Unique auto-incrementing identifier for the record."
    },
    "word": {
      "type": "string",
      "maxLength": 255,
      "pattern": "^[\\p{L}\\p{M}\\s'-]+$",
      "description": "The Filipino profanity word (must be unique)."
    },
    "created_at": {
      "type": ["string", "null"],
      "format": "date-time",
      "description": "Timestamp when the record was created."
    },
    "updated_at": {
      "type": ["string", "null"],
      "format": "date-time",
      "description": "Timestamp when the record was last updated."
    }
  },
  "required": ["word"],
  "additionalProperties": false,
  "examples": [
    {
      "id": 1,
      "word": "bobo",
      "created_at": "2025-11-03T10:00:00Z",
      "updated_at": "2025-11-03T12:00:00Z"
    }
  ]
}
